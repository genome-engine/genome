package temp_env

import "github.com/genome-engine/genome/engine/units"

func (e *Env) exist(name string, unit units.Unit) bool {
	switch name {
   	{{range .}}
   	case {{lower .GetName}}s:
		for _, field := range e.{{.GetName}}s {
			if field.GetId() == unit.GetId(){
				return true
			}
		}
	{{end}}
	}

	return false
}